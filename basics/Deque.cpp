#include<iostream>

using namespace std;

/*
Deque is a extension to Queue that allows insertion and deletion at both ends. It can act as a FIFO Queue, as well as LIFO stack.

More Versatile than Queue : Provides extra methods like addFirst(), addLast(), peekFirst(), peekLast().

Constant O(1) complexity for adding and removing at both ends.
*/

class Deque {
    private:

    public:
};

int main() {
    
    return 0;
}