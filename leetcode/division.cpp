#include<iostream>
#include<cmath>

using namespace std;

class Solution {
public:
    int divide(int dividend, int divisor) {
        bool isNum = (dividend < 0)? true : false,
            isDin = (divisor < 0)? true : false;

        dividend = abs(dividend);
        divisor = abs(divisor);

        
    }
};

int main() {
    
    return 0;
}